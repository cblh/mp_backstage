<h1>添加新自动回复</h1>
<%= form_for @keyword do |f| %>
	<% if @keyword.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(@keyword.errors.count, 'error') %> prohibited this keyword from being saved: </h2>
		<ul>
			<% @keyword.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end -%>
		</ul>
	</div>
	<% end -%>
	<p>
		<%= f.label :keyword %><br>
		<%= f.text_field :keyword %>
	</p>
	<p>
		<%= f.label :resource_id %><br>
		<%= select_tag :resource_id, options_from_collection_for_select(@resources, "id", "title") %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end -%>

<%= link_to '返回', resources_path %>